import styled from 'styled-components'

const DisplayOneSale = styled.div`
    max-width: 250px;
    img {
        margin-top: 20px;
    }
    transition: all 500ms ease-in-out;
    &:hover {
        transform: scale(1.1);
    }

`

export default DisplayOneSale;